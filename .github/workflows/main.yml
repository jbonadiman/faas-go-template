name: ci/cd

on:
    push:
        branches: [ main ]
    pull_request:
        branches: [ main ]

jobs:
  build:
    uses: ./.github/workflows/build.yml
      
  deploy:
    uses: ./.github/workflows/deploy.yml
    
